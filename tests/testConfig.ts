// This file can hold configuration constants for tests.
// For example, if we were using a file-based test database:
// export const DB_FILE_PATH_TEST = './test_trading_data.db';

// For now, it can be empty or export other relevant test configurations.
export const TEST_API_KEY_EXAMPLE = 'YOUR_TEST_API_KEY';
